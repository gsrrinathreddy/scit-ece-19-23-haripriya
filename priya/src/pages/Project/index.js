import { Typography } from '@mui/material';

export default function Project(){
    return(
        <>
        < Typography>
        Project : Face Detection and Recognition with python
        </Typography>
        <Typography>project : Automatic car parking slot indicator</Typography>
        </>
    )
    }